# Recall MCP Server Environment Variables
#
# This is a template for the environment variables needed to run the Recall MCP Server.
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#   chmod 600 .env  (to secure the file)

# Required - Your Recall private key
# Can be provided with or without the "0x" prefix - both formats work
RECALL_PRIVATE_KEY=your_private_key_here

# Optional - Network to connect to (defaults to testnet if not specified)
# Valid options: testnet, mainnet
RECALL_NETWORK=testnet

# Optional - Bucket alias for storing sequential thinking logs
# Defaults to 'sequential-thinking-logs' if not specified
# RECALL_BUCKET_ALIAS=sequential-thinking-logs

# Optional - Prefix for log files stored in the bucket
# Defaults to 'sequential-thinking-' if not specified
# RECALL_LOG_PREFIX=sequential-thinking-

# Optional - Enable debug output
# Set to "true" to see additional debug information
# DEBUG=true
